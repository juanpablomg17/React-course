import { PROMOTIONS } from '../../Components/data/promotions';


export const Promotions = (state = PROMOTIONS, action) => {
    switch(action.type) {
        default: 
            return state; 
    }
}